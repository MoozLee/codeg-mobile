{
  "name": "@codeg/relay",
  "version": "0.1.0",
  "private": true,
  "description": "codeg zero-trust WebSocket relay on Cloudflare Workers + Durable Objects",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.30",
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9",
    "wrangler": "^3.84.0"
  }
}
